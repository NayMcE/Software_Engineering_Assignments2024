# create a function in python that can take a list of numbers squares each number passed into it and then sums the results together.
from operator import truediv


# def square_sum(numbers):
#     return sum(x ** 2 for x in numbers)
#
# numbers = [1, 2, 2]
# result = square_sum(numbers)
# print (result)

def greet(name, owner):
    if name = owner:
        print(f'hello boss')
    else:
        print('Hello guest')

